# Python OOP E-Commerce Project

This is a simple e-commerce system built using Python and Object Oriented Programming concepts.

The project simulates a basic shopping flow where a user adds products to a cart, places an order, and completes payment using different payment methods.

Concepts used: Classes, Encapsulation, Abstraction, and Polymorphism.

Run the project using:
python main.py
